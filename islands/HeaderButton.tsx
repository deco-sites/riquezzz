import Buttons from "$store/components/header/Buttons.tsx";
import type { Props } from "$store/components/header/Buttons.tsx";

export default function ButtonsIsland(props: Props) {
  return <Buttons {...props} />;
}
